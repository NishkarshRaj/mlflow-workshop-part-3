FROM continuumio/miniconda:4.5.4

RUN pip install mlflow>=1.0 \
    && pip install pandas==0.22.0 \
    && pip install scikit-learn==0.19.1 \
    && pip install scipy 
    
RUN conda install -y sqlite

ADD . .
RUN python run_weather_forecast.py
